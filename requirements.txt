# PyTorch: install separately for your CUDA version (Savio / local GPU).
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# CPU-only: pip install torch
#
# Then:
#   pip install -r requirements.txt

numpy>=1.20
pandas>=1.3
pyarrow>=10.0
matplotlib>=3.5
wandb>=0.15
